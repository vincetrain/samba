[global]
        security = user

        load printers = no
        printing = bsd
        printcap name = /dev/null

        invalid users = root
        restrict anonymous = 2
        map to guest = Never

        disable spoolss = yes
        disable netbios = yes
        smb ports = 445

	server smb encrypt = required
        server min protocol = SMB3
        server signing = mandatory

        client min protocol = SMB3
        client smb encrypt = required
        client signing = required
[homes]
        path = /storage/%S
        comment = %S's share
        valid users = %S
        browseable = no
        writable = yes
        read only = no
	create mask = 0600
	directory mask = 0600
[ipc$]
        # disable ipc / disallow ipc connections
        hosts deny = 0.0.0.0/0